import {ListColumnDirective} from './list-column.directive';

describe('ListColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new ListColumnDirective();
    expect(directive).toBeTruthy();
  });
});
