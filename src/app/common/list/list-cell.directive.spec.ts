import {ListCellDirective} from './list-cell.directive';

describe('ListCellDirective', () => {
  it('should create an instance', () => {
    const directive = new ListCellDirective();
    expect(directive).toBeTruthy();
  });
});
